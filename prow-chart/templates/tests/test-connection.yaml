apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prow-chart.fullname" . }}-test-connection"
  labels:
    {{- include "prow-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prow-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
